[project]
name = "ghostpay"
version = "1.1.0"
description = "Per-subscription virtual cards, price hike detection and churn analytics"
authors = [{ name = "Buse Bozyel" }]
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py311"
